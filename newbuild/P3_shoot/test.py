import pygame
from global_inst import WHITE
#from menu import Menu


'''def test(score_list):
    pygame.init()
    t = pygame.display.set_mode((100, 100))
    t.fill(WHITE)
    intro = True
    while intro:
        for event in pygame.event.get():
            if event.type == pygame.QUIT:
                intro = False
                pygame.quit()
                Menu(score_list)
        pygame.display.update()
    pygame.quit()'''
